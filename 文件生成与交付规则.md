# 文件生成与交付规则

生成 HTML、报表、文档、数据导出等文件时，必须遵守以下规则。

## 一、文件保存

1. 必须先使用 `write` 将文件保存到 `/opt/.openclaw/workspace/` 目录或其子目录。
2. 必须使用完整的绝对路径，路径中不得包含 `..`。
3. HTML 文件必须保存为 `/opt/.openclaw/workspace/<name>.html`。
4. 不得依赖 `[embed]` 内联展示 HTML。`canvasHost` 服务可能未启用，使用 `[embed]` 可能导致渲染失败。
5. 必须确认文件保存成功后，才能向用户交付。

## 二、按渠道交付

### WebChat

1. 禁止使用 `MEDIA:` 前缀投递文件。
2. 必须在回复正文中提供文件的完整绝对路径。
3. 路径必须使用 Markdown 链接格式：

   ```text
   [文件名](/opt/.openclaw/workspace/文件名)
   ```

4. 示例：

   ```text
   [营销活动报表.xlsx](/opt/.openclaw/workspace/营销活动报表.xlsx)
   ```

### 钉钉和飞书

1. 必须且只能使用 `MEDIA:` 投递文件。
2. 禁止使用 Markdown 路径链接交付文件。
3. `MEDIA:` 后必须填写文件的完整绝对路径。
4. 示例：

   ```text
   MEDIA:/opt/.openclaw/workspace/营销活动报表.xlsx
   ```

## 三、交付校验

交付文件前，必须确认：

- 文件已实际生成并保存成功。
- 交付路径与实际文件路径完全一致。
- WebChat 使用完整绝对路径的 Markdown 链接，且不含 `MEDIA:` 前缀。
- 钉钉和飞书仅使用 `MEDIA:` 投递，不使用 Markdown 路径链接。

